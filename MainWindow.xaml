<Window x:Class="MyCanBusTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyCanBusTool"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Name="BtnStart" Content="Start CAN (10ms Cycle)" Click="BtnStart_Click" Width="150" Margin="0,0,10,0"/>
            <Button Name="BtnStop" Content="Stop" Click="BtnStop_Click" Width="100"/>
            <TextBlock Name="TxtStatus" Text="Disconnected" VerticalAlignment="Center" Margin="20,0,0,0" FontWeight="Bold"/>
        </StackPanel>

        <DataGrid Name="DgMessages" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="100"/>
                <DataGridTextColumn Header="ID (Hex)" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50"/>
                <DataGridTextColumn Header="Data" Binding="{Binding Data}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
